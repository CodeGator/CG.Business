using System;

namespace CG.Business.Managers
{
    /// <summary>
    /// This interface represents an object that wraps other stores,
    /// or services, and builds up complexity by reusing the capabilities
    /// of those objects.
    /// </summary>
    public interface IManager : IDisposable
    {

    }
}
