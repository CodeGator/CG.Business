using System;

namespace CG.Business.Directors
{
    /// <summary>
    /// This interface represents an object that wraps other managers, 
    /// and builds up complexity by reusing the capabilities of those objects.
    /// </summary>
    public interface IDirector : IDisposable
    {

    }
}
